uploadArchives {
    repositories {
        mavenDeployer {
            pom.artifactId = "com.etsy.android.grid"
            pom.version = "1.0.3"
        }
    }
}

publishing {
    publications {
        bar(MavenPublication) {
            version = "1.0.3"
        }
    }
}