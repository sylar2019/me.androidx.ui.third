uploadArchives {
    repositories {
        mavenDeployer {
            pom.artifactId = "com.komodo.tagin"
            pom.version = "1.0"
        }
    }
}

publishing {
    publications {
        bar(MavenPublication) {
            version = "1.0"
        }
    }
}
