uploadArchives {
    repositories {
        mavenDeployer {
            pom.artifactId = "com.viewpagerindicator"
            pom.version = "2.4.1"
        }
    }
}

publishing {
    publications {
        bar(MavenPublication) {
            version = "2.4.1"
        }
    }
}
