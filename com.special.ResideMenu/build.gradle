uploadArchives {
    repositories {
        mavenDeployer {
            pom.artifactId = "com.special.ResideMenu"
            pom.version = "1.4.2"
        }
    }
}

publishing {
    publications {
        bar(MavenPublication) {
            version = "1.4.2"
        }
    }
}
