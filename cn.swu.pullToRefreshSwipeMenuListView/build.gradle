uploadArchives {
    repositories {
        mavenDeployer {
            pom.artifactId = "cn.swu.pulltorefreshswipemenulistview"
            pom.version = "1.0.0"
        }
    }
}

publishing {
    publications {
        bar(MavenPublication) {
            version = "1.0.0"
        }
    }
}