uploadArchives {
    repositories {
        mavenDeployer {
            pom.artifactId = "cn.trinea.android.view.autoscrollviewpager"
            pom.version = "1.1.1"
        }
    }
}

publishing {
    publications {
        bar(MavenPublication) {
            version = "1.1.1"
        }
    }
}
